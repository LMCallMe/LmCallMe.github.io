# libreoffice 的短名为 soffice 或 loffice
# 若要用短名 office代替，则需要下面的命令
# echo "alias office=libreoffice" >> ~/.bashrc
# 解释： 将 "alias office=libreoffice" 追加到 ~/.bashrc 的最后一行

# 安装 wps
sudo dpkg -i wps.deb
# 运行 wps 用命令 wps
# 缺少必要的Symbol字体：
# 下载字体 wingdings、wingdings 2、wingdings 3、Webdings、MT Extra
# 进入主文件夹（Ctrl+h），查看 .fonts 文件夹，没有就新建
# 将必要的字体复制到 .fonts 文件夹
if [ ! -d "~/.fonts" ]; then
  mkdir ~/.fonts
fi
cp ./fonts/*.ttf ~/.fonts
